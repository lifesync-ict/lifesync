import type { LanguageCode } from '../../data/translations'
import type { HandoffResult } from './types'
export function buildHandoffBundle(result: HandoffResult, included: Record<string, boolean>, language: LanguageCode, privacyNotice: string) { return { schemaVersion: '1.0.0', generatedAt: new Date().toISOString(), language, demoOnly: true, privacyNotice, verificationStatus: 'review_required', selectedItems: result.evidenceBundle.filter((item) => included[item.id] ?? item.included), institutions: [result.primaryInstitution, ...result.alternativeInstitutions].filter(Boolean).map((item) => ({ type: item?.type, verificationStatus: item?.verificationStatus, jurisdiction: item?.jurisdiction, address: item?.address, phone: item?.phone, sourceUrl: item?.sourceUrl })) } }
export function downloadJson(data: unknown) { const blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json;charset=utf-8' }); const url = URL.createObjectURL(blob); const anchor = document.createElement('a'); anchor.href = url; anchor.download = 'lifesync-handoff-summary.json'; anchor.click(); URL.revokeObjectURL(url) }
export function summaryText(data: unknown) { return JSON.stringify(data, null, 2) }
